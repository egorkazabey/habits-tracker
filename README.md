# Привычки — Telegram Mini App

Трекер привычек внутри Telegram. Данные каждого пользователя хранятся
через `Telegram.WebApp.CloudStorage` — облачное хранилище, привязанное
к паре «бот + аккаунт пользователя». Отдельный backend и база данных
не нужны, поэтому размещение приложения ничего не стоит: нужен только
бесплатный статический хостинг для файлов фронтенда.

Вне Telegram (`npm run dev` в обычном браузере) приложение автоматически
переключается на `localStorage`, чтобы можно было разрабатывать и
проверять UI без бота.

## Локальный запуск

```bash
npm install
npm run dev
```

## Деплой (бесплатно)

Подходит любой статический хостинг с HTTPS, например Vercel или
GitHub Pages.

**Vercel:**
1. Залить репозиторий на GitHub.
2. На vercel.com → New Project → выбрать репозиторий (framework определится как Vite автоматически).
3. Deploy — получите постоянный URL вида `https://<project>.vercel.app`.

**GitHub Pages:**
1. В `vite.config.ts` добавить `base: '/<repo-name>/'`.
2. `npm run build`, задеплоить содержимое `dist/` через Pages (например, `gh-pages` пакетом или GitHub Actions).

## Настройка бота (BotFather)

1. В Telegram открыть `@BotFather` → `/newbot`, задать имя и юзернейм — получите токен бота (для базовой настройки сервер не нужен; токен также потребуется ниже, если захотите включить push-напоминания).
2. `/mybots` → выбрать бота → `Bot Settings` → `Menu Button` → `Configure Menu Button` → указать задеплоенный HTTPS URL. Это откроет приложение по кнопке рядом с полем ввода в чате с ботом.
3. Либо `/newapp`, чтобы создать полноценное Mini App с собственной иконкой и именем в списке приложений.

После этого у каждого пользователя, открывшего приложение через бота,
данные о привычках будут сохраняться в его Telegram-аккаунте и
загружаться на любом устройстве, где он открыт в Telegram.

## Напоминания через бота (опционально)

Привычки можно снабдить временем напоминания в форме создания — по
умолчанию это просто хранится в CloudStorage и ни на что не влияет.
Чтобы напоминания реально приходили сообщением от бота в нужное время,
нужен отдельный бесплатный бэкенд — `CloudStorage` доступен только
изнутри самого Mini App и не читается внешним cron-заданием.

Бэкенд лежит в [`worker/`](worker) — это **Cloudflare Worker** с
**D1** (бесплатная SQLite-база) и **Cron Trigger**, запускающийся
каждую минуту. Всё в одном бесплатном аккаунте Cloudflare, отдельная
СУБД не нужна.

**Деплой:**
```bash
cd worker
npm install
npx wrangler login
npx wrangler d1 create habits-reminders   # выведет database_id
# вставьте database_id в wrangler.toml
npx wrangler d1 execute habits-reminders --remote --file=schema.sql
npx wrangler secret put BOT_TOKEN          # вставьте токен из BotFather
npx wrangler deploy                        # выведет https://*.workers.dev URL
```

Затем укажите этот URL как `VITE_REMINDERS_API_URL` в окружении сборки
фронтенда (например, переменная окружения проекта в Vercel) и
пересоберите/передеплойте Mini App. После этого форма «Напоминания» в
приложении начнёт реально регистрировать уведомления.

Что не реализуется в принципе: выбор мелодии напоминания («Ringtone» из
референса) — Bot API отправляет обычное сообщение, звук уведомления
берётся из настроек Telegram у самого пользователя, а не задаётся ботом.

## Ограничения CloudStorage

- До 1024 ключей, до 4096 символов на значение — для личного трекера
  привычек этого хватает на годы истории (данные по месяцам хранятся
  отдельными ключами).
- Данные видны только этому боту и не синхронизируются с другими ботами
  или обычным Telegram Cloud.
